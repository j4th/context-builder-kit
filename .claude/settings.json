{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "_comment_hooks": "Guards in FOUR tiers; each tier names its mechanism and its dated source; prerequisites and the canonical dry-run are stated once at the end (cbk-conventions-reference.md § HITL gate load-bearing heuristics › Mechanize the gates; the authoring shape is § Hook authoring). Every command uses ${CLAUDE_PROJECT_DIR}/… — handlers run in the current directory (code.claude.com/docs/en/hooks, verified 2026-09-06), so a bare relative path is not found from a subdirectory. HARD-DENY (PreToolUse, exit 2) for actions never legitimate for the agent: protect-immutable-adrs.sh blocks Edit/Write/MultiEdit on existing docs/adr/NNNN-*.md; protect-lock-files.sh blocks edits to lock files (change them via the package manager); protect-main-branch.sh blocks `git commit` on main/master (branch-first per cbk-conventions.md § Branch naming; judged on the branch at entry, so branch creation and the first commit are separate calls); require-repo-root-for-agents.sh blocks Task/Agent/Workflow dispatch from any directory but the checkout's git top-level (the memory-root mechanism and its dated source are stated once in pr-review.md § Reviewer precedent memory); the `Task|Agent|Workflow` matcher is a dated observation of the tool names the harness reports, with the older name kept so a rename degrades to a no-op. ASK-GATE (PreToolUse, permissionDecision \"ask\") for one-way doors legitimate only when operator-instructed — the permission prompt IS the per-action HITL approval, and it fires even when a broad allowlist entry would otherwise auto-approve: guard-pr-state.sh intercepts gh pr ready/merge/close/reopen; require-knowledge-backend-ok.sh intercepts knowledge-backend MCP writes (Notion matcher shipped as the v1 reference — adjust the regex to your MCP's tool names; when the knowledge axis is `none` the guard is inert because its tools are not connected, and it follows the rule file's disposition at bootstrap). ADVISORY (PostToolUse, exit 0 always) ships as two unregistered exemplars, format-on-edit.sh and analyze-on-edit.sh (the analyzer runs the project's analyzer on the edited file's package, errors only; measured under a second per package on a real run, 2026-09-05); each hook's header carries the registration stanza to copy into hooks.PostToolUse once its case arms are wired. NO exemplar object lives in this file: a top-level key holding a matcher/hooks object makes the harness discard the whole settings file — every guard, enabledMcpjsonServers and enabledPlugins inert, silently (Claude Code 2.1.270–2.1.278; single-variable probe on a real application 2026-09-13, the parser's fatal diagnostic read from the 2.1.278 bundle 2026-09-21), and the verification block asserts none is present. STOP (Stop, exit 2 blocks the stop once): detect-forked-agent-memory.sh exits 2 while a `.claude/agent-memory/` or `.claude/agent-memory-local/` tree exists anywhere but the root (both memory scopes fork the same way), so the agent that forked it moves it before handing off; `stop_hook_active` prevents a loop, and the harness overrides a Stop hook after eight consecutive blocks (code.claude.com/docs/en/hooks-guide § Limitations and troubleshooting, verified 2026-09-06). Registered on Stop only: on SubagentStop it would block every finishing subagent, which has no hand-off. All guards fail open (exit 0 + stderr warning naming the surviving backstop) on environment defects, so a missing jq can't turn a targeted guard into a universal block. Prerequisites: jq, git, bash 3.2+. Canonical dry-run: pipe a crafted JSON payload into the script and assert the exit (the verification block runs the launch-root guard's payloads on every run; the Stop hook's fixture run is the procedure in cbk-conventions-reference.md § Hook authoring › Verify by payload, recorded in the PR body's table).",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PROJECT_DIR}/.claude/hooks/protect-immutable-adrs.sh"
          },
          {
            "type": "command",
            "command": "${CLAUDE_PROJECT_DIR}/.claude/hooks/protect-lock-files.sh"
          }
        ]
      },
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PROJECT_DIR}/.claude/hooks/protect-main-branch.sh"
          },
          {
            "type": "command",
            "command": "${CLAUDE_PROJECT_DIR}/.claude/hooks/guard-pr-state.sh"
          }
        ]
      },
      {
        "matcher": "mcp__(plugin_Notion_)?notion__notion-(create|update|move|duplicate|convert|delete).*",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PROJECT_DIR}/.claude/hooks/require-knowledge-backend-ok.sh"
          }
        ]
      },
      {
        "matcher": "Task|Agent|Workflow",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PROJECT_DIR}/.claude/hooks/require-repo-root-for-agents.sh"
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PROJECT_DIR}/.claude/hooks/detect-forked-agent-memory.sh"
          }
        ]
      }
    ]
  },
  "_comment_enabledPlugins": "Plugins the cascade kit expects. Install each via Claude Code's plugin system before running /finish: pr-review-toolkit (the review pass dispatches pr-review-toolkit:review-pr) and commit-commands (staging/commit wrappers used by the kit's examples). `/simplify` — /finish's simplify pass — is built into Claude Code as a bundled skill (verified against the installed CLI bundle, Claude Code 2.1.263, 2026-09-06; re-verify after harness upgrades) and is deliberately NOT listed here: add \"simplify\": true only if your install ships it as a plugin. Rename entries to match installed plugin IDs if yours differ.",
  "enabledPlugins": {
    "pr-review-toolkit": true,
    "commit-commands": true
  },
  "_comment_enabledMcpjsonServers": "MCP servers the cascade reads from .mcp.json. Copy .mcp.json.example to .mcp.json and configure the credentials/endpoints for each server you want enabled. Comment out any server you don't use.",
  "enabledMcpjsonServers": [
    "github",
    "linear",
    "notion",
    "context7",
    "time"
  ]
}
